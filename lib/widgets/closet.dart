import 'package:flutter/material.dart';

class ClosetScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Closet Screen'),
    );
  }
}


